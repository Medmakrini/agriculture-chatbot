nix:
  setup: |
    nix-env -i python38 gcc ffmpeg
    pip install -r requirements.txt
    python3 -m nltk.downloader stopwords
  install: |
    pip install -r requirements.txt
  start: |
    gunicorn main:app --bind 0.0.0.0:$PORT
